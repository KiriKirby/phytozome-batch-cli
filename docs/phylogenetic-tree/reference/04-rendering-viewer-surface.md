# Rendering And Export Surface

MEGA's Tree Explorer is the behavioral reference for tree display, interaction,
and export. PHgo does not embed MEGA GUI widgets; the browser viewer adapts the
same user-facing concepts to Reactree and PHgo's local viewer service.

Primary sources:

- `_mega_source/MEGA12.1-source/TreeEditor/`
- `_mega_source/MEGA12.1-source/MegaVcl/mtreebox.pas`
- `MEGA12_HTML_DIR/tree_options_tree.html`
- `MEGA12_HTML_DIR/tree_options_branch.html`
- `MEGA12_HTML_DIR/tree_options_labels.html`
- `MEGA12_HTML_DIR/tree_options_scale.html`
- `MEGA12_HTML_DIR/tree_options_cutoff.html`
- `MEGA12_HTML_DIR/subtree_drawing_options.html`
- `MEGA12_HTML_DIR/newick_export.html`
- `_mega_source/MEGA12.1-source/TreeEditor/msvgtreerenderer.pas`
- `_mega_source/MEGA12.1-source/TreeEditor/mpdftreerenderer.pas`
- `_mega_source/MEGA12.1-source/TreeEditor/mbitmaptreerenderer.pas`
- `tree-viewer/src/`
- `internal/phylo/viewer.go`

## Viewer Ownership

The viewer owns:

- rendering Newick trees
- displaying aligned FASTA beside the tree
- searching labels
- visual editing supported by Reactree
- relabeling runtime taxon IDs to display names
- adapting MEGA runtime Newick into MEGA Tree Explorer's default display order
- SVG/PNG/PDF export
- `.pgv` viewer snapshot export
- browser-local layout and interaction state

The viewer must not:

- align sequences
- infer trees
- bootstrap trees
- repair Newick generated by MEGA except for display-label remapping and
  MEGA Tree Explorer default display ordering at the adapter boundary
- invent branch lengths or support values

## MEGA Tree Layout Options Inventory

Source: `tree_options_tree.html`

| MEGA Section | Control | Source ID | PHgo Viewer Obligation |
| --- | --- | --- | --- |
| Rectangular Tree | Taxon Separation | `tree_options_rect_taxon_separation` | map to vertical leaf spacing when layout supports it |
| Rectangular Tree | Tree Width | `tree_options_rect_width` | map to tree width/scale controls |
| Circle Tree | Display Taxon Name Horizontally | `tree_options_tree_taxon_name_circle` | map to circular label orientation when available |
| Circle Tree | Start Angle | `tree_options_circle_start_angle` | map to circular layout rotation |
| Circle Tree | Radius | `tree_options_circle_radius` | map to circular radius/zoom |
| Circle Tree | Center Hole | `tree_options_circle_center_hole` | map to radial hole/inner radius if supported |
| Radiation Tree | Display Taxon Name Horizontally | `tree_options_tree_taxon_name_rad` | map to radial label orientation when available |
| Radiation Tree | Branch Length | `tree_options_rad_branch_length` | map to radial branch-length scaling |
| Radiation Tree | Start Angle | `tree_options_rad_start_angle` | map to radial rotation |

Reactree may expose these through different controls, but each supported layout
concept must have a documented mapping or a tracked gap.

## Branch Options Inventory

Source: `tree_options_branch.html`

| MEGA Section | Control | Source ID | Notes |
| --- | --- | --- | --- |
| General | Line Width | `tree_options_branch_lines` | values `1 pt` through `5 pt` |
| Statistics/Frequency | Display Statistics/Frequency | `tree_options_branch_display_stats` | support/bootstrap label visibility |
| Statistics/Frequency | Placement | `tree_options_branch_stat_placement` | `Automatic`, `Above Branch`, `Below Branch` |
| Statistics/Frequency | Font | `tree_options_branch_statistics_font` | font style/color mapping |
| Statistics/Frequency | Horizontal | `tree_options_branch_horizontal` | label offset |
| Statistics/Frequency | Vertical | `tree_options_branch_vertical` | label offset |
| Statistics/Frequency | Hides values lower than | `tree_options_branch_hide_lower`, `tree_options_branch_hide_values` | support cutoff display |
| Branch Length | Display Branch Length | `tree_options_branch_display_branch` | branch-length label visibility |
| Branch Length | Placement | `tree_options_branch_branch_placement` | `Automatic`, `Above Branch`, `Below Branch` |
| Branch Length | Font | `tree_options_branch_length_font` | font style/color mapping |
| Branch Length | Precision | `tree_options_branch_precision` | decimal places |
| Branch Length | Hide if shorter than | `tree_options_branch_hide_shorter`, `tree_options_hide_shorter` | branch-length cutoff display |
| Divergence Time | Display Divergence Times | `tree_options_branch_display_divergence` | only meaningful for timetrees |
| Divergence Time | Placement | `tree_options_branch_time_placement` | `Automatic`, `Above Branch`, `Below Branch` |
| Divergence Time | Font | `tree_options_branch_divergence_font` | font style/color mapping |
| Divergence Time | Precision | `tree_options_branch_divergence_precision` | decimal places |
| Divergence Time | Horizontal/Vertical | `tree_options_branch_distance_horizontal`, `tree_options_branch_distance_vertical` | label offset |

PHgo's initial system-tree scope may omit timetree-only divergence controls, but
the omission must be explicit because MEGA Tree Explorer includes them.

## Label And Marker Options Inventory

Source: `tree_options_labels.html`

| Control | Source ID | Default / Values |
| --- | --- | --- |
| Display Taxon Names | `tree_options_labels_display_taxon_names` | checked by default |
| Taxon label font | `tree_options_branch_labels_font` | font picker |
| Taxon label color | `tree_options_branch_labels_color` | `#000000` default |
| Display Taxon Markers | `tree_options_labels_display_taxon_markers` | checked by default |
| Marker Shape | `tree_options_labels_shape_options` | `msNone`, open/filled circle, square, up/down triangle, diamond |
| Marker Color | `marker_color_hidden` | `#000000` default |
| Per-taxon marker list | `taxon_markers_list` | row-level marker customization |

PHgo must preserve arbitrary visible display names, including underscores,
parentheses, commas, semicolons, colons, and single quotes. Runtime taxon IDs
remain stable (`PHGOT...`); viewer labels are a collision-safe mapping layer.

## Scale Options Inventory

Source: `tree_options_scale.html`

| MEGA Section | Control | Source ID |
| --- | --- | --- |
| Scale format | Line Width | `tree_options_scale_lines` |
| Scale format | Font | `tree_options_scale_font` |
| Distance Scale | Show | `tree_options_scale_distance_scale` |
| Distance Scale | Caption | `tree_options_scale_name_caption_distance_scale` |
| Distance Scale | Scale length | `tree_options_distance_scale_length` |
| Distance Scale | Tick interval | `tree_options_distance_tick_interval` |
| Time Scale | Show | `tree_options_scale_time_scale` |
| Time Scale | Caption | `tree_options_scale_name_caption_time_scale` |
| Time Scale | Major tick interval | `tree_options_scale_major_tick` |
| Time Scale | Minor tick interval | `tree_options_scale_minor_tick` |
| Time Scale | Show Node Height Error Bar (2SE) | `tree_options_scale_node_height_err` |

Distance scale controls are in scope for ordinary system trees. Time-scale
controls are timetree-specific and should be tracked separately unless PHgo
adds timetree inference.

## Cutoff Options Inventory

Source: `tree_options_cutoff.html`

| Control | Source ID | Default |
| --- | --- | --- |
| Cut-off Value for Condensed Tree | `tree_options_cutoff_condensed` | `50` |
| Cut-off Value for Consensus Tree | `tree_options_cutoff_consensus` | `50` |

These are display/derived-tree controls. PHgo should not condense or consensus
trees in Go unless the operation is routed through MEGA runtime behavior.

## Newick Export Options Inventory

Source: `newick_export.html`

| Control | Source ID |
| --- | --- |
| Branch Lengths | `newick_branch_lengths` |
| Bootstrap Values | `newick_bootstrap_values` |
| Node Labels | `newick_node_labels` |
| Gene Duplications | `newick_gene_duplications` |
| Speciations | `newick_speciations` |
| Relative Times | `newick_relative_times` |
| Divergence Times | `newick_divergence_times` |

Runtime `tree.nwk` is the primary inferred tree artifact. Viewer export may
offer user-facing export variants, but any biological or topology-changing
export variant must be backed by MEGA runtime semantics.

## Current PHgo Viewer Mapping

| Area | PHgo Component | Status |
| --- | --- | --- |
| Viewer service | `internal/phylo/viewer.go` | local session service |
| Browser renderer | `tree-viewer/src/main.jsx` | Reactree-backed |
| MEGA default display order | `tree-viewer/src/mega-display.js` | midpoint-root plus balanced-shape ordering before Reactree |
| Label map | `tree-viewer/src/labels.js` | stable ID to display-name mapping |
| Newick quoted labels | `tree-viewer/scripts/patch-reactree-dpi.mjs` | patches parser/exporter |
| Alignment panel | `tree-viewer/src/main.jsx`, CSS | displays aligned FASTA from runtime |
| SVG/PNG/PDF export | `tree-viewer/src/pgv.js`, patched Reactree export helpers | browser-side graphical export |
| Viewer snapshot save | `tree-viewer/src/pgv.js`, `internal/viewersnapshot/` | `.pgv` payload/state |
| Viewer snapshot open | `internal/workflow/newick_browser.go`, `tree-viewer/src/pgv.js` | `.pgv` can be opened through the standalone tree browser path; the Canvas viewer page no longer exposes a top-right local file button |

Current export plumbing:

- Reactree's download menu is patched by
  `tree-viewer/scripts/patch-reactree-dpi.mjs` to route SVG, PNG, and PDF
  commands through PHgo browser save bridges instead of hidden ad hoc download
  behavior.
- `tree-viewer/src/pgv.js` owns the concrete export functions:
  `exportSVG`, `exportPNG`, and `exportPDF`, with PNG rasterization and PDF
  generation performed in the browser from the rendered SVG.
- `scripts/build-tree-viewer.ps1` runs the Reactree patch, `npm test`,
  `npm run build`, and syncs the resulting assets into
  `internal/phylo/viewer_assets`.
- `internal/phylo/viewer_test.go` verifies Canvas and standalone browser
  sessions keep payload/state isolated by session ID.

## PGV Snapshot Flow

- `buildViewerSnapshot` writes the current viewer payload plus Reactree/browser
  state.
- `parseViewerSnapshot` validates `format=phgo-viewer-snapshot` and schema
  version before accepting the file.
- Opening a `.pgv` is handled by the standalone tree-browser workflow, which
  creates an isolated viewer session from the snapshot payload/state.
- The Canvas viewer page intentionally has no top-right `.pgv` open button, so
  live Canvas sessions stay tied to the current PHgo payload.
- `.pgv` never recomputes alignment/tree data and never writes back to
  `runtime-request.json`, `aligned.fasta`, or `tree.nwk`; it is a render-state
  carrier only.

## Default Display Order

MEGA source anchors:

- `_mega_source/MEGA12.1-source/MegaVcl/mtreebox.pas`
  `TTreeCustomControl.InitTree`, `SearchMidPoint`, and
  `MakeRootOnMidPoint`.
- `_mega_source/MEGA12.1-source/TamuraFuncs/mtreeproc.pas`
  `SortBranchByFigure`.
- `_mega_source/MEGA12.1-source/TreeEditor/mtreeviewform.lfm`
  `ActionBalancedTree.Checked = True`.
- `_mega_source/MEGA12.1-source/TreeEditor/mtreeviewform.pas`
  `DecideRootOnMidpoint` and `InitForm`.

For ordinary inferred NJ/ME/ML/MP trees, MEGA Tree Explorer defaults to rooting
branch-length trees at the midpoint and sorting clusters for balanced shape.
The PHgo runtime artifact `tree.nwk` remains the raw MEGA runtime export; before
passing the tree to Reactree, `tree-viewer/src/mega-display.js` applies the same
display-only default. This is not a computation step and does not alter
`tree.nwk`, `runtime-request.json`, or snapshot computation fingerprints.

Canvas live sessions also discard saved Reactree `treeData/history` on payload
load so stale browser reroot/flip state cannot hide the MEGA-default preview.
Standalone `.pgv` sessions keep their saved Reactree tree state because opening
that file is an explicit snapshot-restore action.

## Audit Requirements

- Maintain an explicit mapping/gap table for every MEGA Tree Explorer display
  option above.
- Confirm Reactree labels and alignment labels round-trip with quoted Newick
  labels and arbitrary display names.
- Browser tests must cover `.pgv` save/open, graphical export plumbing, and
  label remapping.
- Playwright/manual visual checks must verify nonblank desktop and mobile
  rendering after significant viewer changes.
